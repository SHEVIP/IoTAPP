# 应用信息
name: untitled
description: "物联网APP"
version: 1.0.0+1

# 开发环境
environment:
  sdk: '>=3.2.1 <4.0.0'

# 项目依赖
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: 2.0.8
  dio: '>2.1.3'
  http: ^0.13.3
  fl_chart: ^0.36.0
  table_calendar: ^3.0.9
  intl: ^0.18.0

# 开发依赖
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
